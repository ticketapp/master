<div class="row-fluid block_event_min">
    <div class="row-fluid span12 img_min_evnt">
        <img src="assets/img/1.jpg" alt="poikjij" class="min"/>
    </div>
    <div class="row-fluid text_evnt_min" >
        <a href="#/event/{{event.id}}"><h4>{{event.nomEvent}}</h4></a>
        Par
        <a href="#/user/{{user.id}}" class="custom-trigger" data-ng-repeat="user in event.users" data-tooltip-placement="bottom" data-tooltip-html-unsafe="<div><img src='img/2.jpg'/><a href='#/user/{{user.id}}'><h4>{{user.username}}</h4></a></div>" data-tooltip-append-to-body="true" data-tooltip-trigger="mouseover">{{ user.username }},</a>
        <br/>
        <div class="infos_event">
            <i class="icon-calendar"></i>{{ event.dateDebut}}
            <i class="icon-time"></i> {{ event.heureDebut}}<br/>
            <i class="icon-map-marker"></i>
            <a href="#/lieu/{{lieu.id}}" data-ng-repeat="lieu in event.lieux">{{lieu.name}} // {{lieu.ville}}</a>
        </div>
        <br/>
        <div class="description_event_min">
            <a href="#/artiste/{{artiste.id}}" data-ng-repeat="artiste in event.artistes" >{{artiste.name}} </a>
        </div>
        <br/>
        <a class="btn btn-warning btn_event" title="Commentaires"  data-bs-popover="'partials/comments.html'"><i class="icon-white icon-comment"></i></a>
        <a href="#" class="btn btn-warning btn_event" title="Partager"><i class="icon-white icon-share"></i></a>
        <a href="#" class="btn btn-warning btn_event" title="Suivre"> + </a>
        <a href="#" class="btn btn-warning btn_event" title="Acheter des places"><i class="icon-white icon-shopping-cart"></i></a>
    </div>
</div>