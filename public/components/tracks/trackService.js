angular.module('claudeApp').service('TrackService', [ function() {
/*
    class Track {
        constructor(uuid, title, url, platform, thumbnailUrl, artistFacebookUrl, artistName, redirectUrl, confidence,
                    playlistRank, genres) {
            this.uuid = uuid;
            this.title = title;
            this.url = url;
            this.platform = platform;
            this.thumbnailUrl = thumbnailUrl;
            this.artistFacebookUrl = artistFacebookUrl;
            this.artistName = artistName;
            this.redirectUrl = redirectUrl;
            this.confidence = confidence;
            this.playlistRank = playlistRank;
            this.genres = genres;
        }
    }

    /!*this.setStringToSetTrack = function (stringSet) {
        stringSet.map(function (track) {
            return new Track(track)
        })
    }
*!/*/

}]);