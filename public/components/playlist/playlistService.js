angular.module('claudeApp').service('TrackService', [ function() {

   /* class Playlist {
        constructor(uuid, title, tracks) {
            this.uuid = uuid;
            this.title = title;
            this.tracks = tracks;
        }
    }

   /!* this.setStringToSetPlaylist = function (stringSet) {
        stringSet.map(function (playlist) {
            return new Playlist(playlist)
        })
    }*!/*/


}]);